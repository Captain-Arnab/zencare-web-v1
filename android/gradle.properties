org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true
# Kotlin incremental compile breaks when project (e.g. E:) and pub cache (e.g. C:) are on different roots.
kotlin.incremental=false
